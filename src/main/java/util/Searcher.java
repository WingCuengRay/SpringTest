package util;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Searcher {
    private Logger logger = LoggerFactory.getLogger(Searcher.class);

    public Searcher(){
        logger.info("Searcher is being initialized");
    }
}
